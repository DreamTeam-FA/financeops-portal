services:
  - type: web
    name: financeops-portal
    runtime: node
    buildCommand: npm run build
    startCommand: node dist/server.cjs
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
